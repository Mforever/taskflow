{
  "name": "taskflow",
  "version": "1.0.0",
  "description": "TaskFlow - современный трекер задач",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^8.0.5"
  }
}
